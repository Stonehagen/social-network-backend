const { fakeUserData, fakeUserData2 } = require('./fakeUserData');
const { fakeLoginData } = require('./fakeLoginData');
const { fakeProfileData } = require('./fakeProfileData');
const { fakePostData } = require('./fakePostData');

module.exports = {
  fakeUserData,
  fakeUserData2,
  fakeLoginData,
  fakeProfileData,
  fakePostData,
};
